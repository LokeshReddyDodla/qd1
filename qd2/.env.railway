# Railway Environment Variables Template
# Copy these to Railway Dashboard → Your Service → Variables

# ============================================================================
# REQUIRED: Qdrant Cloud Configuration
# ============================================================================
QDRANT_HOST=your-cluster-id.aws.cloud.qdrant.io
QDRANT_PORT=6333
QDRANT_API_KEY=your-qdrant-api-key-from-cloud-dashboard
QDRANT_USE_HTTPS=true
QDRANT_COLLECTION_NAME=people_data
QDRANT_VECTOR_SIZE=1536

# ============================================================================
# REQUIRED: OpenAI Configuration
# ============================================================================
OPENAI_API_KEY=sk-proj-your-openai-api-key-here
OPENAI_EMBEDDING_MODEL=text-embedding-3-small
OPENAI_LLM_MODEL=gpt-4o-mini
OPENAI_LLM_TEMPERATURE=0.1
OPENAI_LLM_MAX_TOKENS=1000

# ============================================================================
# Application Configuration
# ============================================================================
LOG_LEVEL=INFO
ENVIRONMENT=production
EMBEDDING_BATCH_SIZE=32
INGEST_BATCH_SIZE=100
DEFAULT_TOP_K=10
MAX_TOP_K=50

# ============================================================================
# OPTIONAL: PostgreSQL (for patient name resolution)
# Only needed if you want to store patient names separately
# ============================================================================
# POSTGRES_HOST=your-postgres-host.railway.internal
# POSTGRES_PORT=5432
# POSTGRES_DB=patient_db
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=your-secure-password

# ============================================================================
# OPTIONAL: MongoDB (for storing source documents)
# Only needed if you want to store original documents
# ============================================================================
# MONGO_URI=mongodb://your-mongo-uri
# MONGO_DB=patient_data

# ============================================================================
# Railway-Specific (Auto-set by Railway)
# ============================================================================
# PORT - Railway sets this automatically, don't add it manually
# RAILWAY_STATIC_URL - Auto-set
# RAILWAY_PUBLIC_DOMAIN - Auto-set

# ============================================================================
# Quick Copy for Railway Dashboard
# ============================================================================
# 1. Go to: https://railway.app/project/YOUR_PROJECT/service/YOUR_SERVICE/variables
# 2. Click "Raw Editor" or "Bulk Import"
# 3. Paste the variables below (update with your actual values)
# 4. Click "Save"

# Minimal required variables (fill in your actual values):
QDRANT_HOST=
QDRANT_API_KEY=
QDRANT_USE_HTTPS=true
OPENAI_API_KEY=
QDRANT_COLLECTION_NAME=people_data
LOG_LEVEL=INFO
ENVIRONMENT=production
